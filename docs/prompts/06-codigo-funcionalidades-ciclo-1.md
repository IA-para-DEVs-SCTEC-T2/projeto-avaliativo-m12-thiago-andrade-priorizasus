# Prompts: Código das Funcionalidades Principais

Prompt 1 — Gerar código das funcionalidades principais com IA (ciclo 1)
---
Contexto: O projeto PRIORIZASUS já tem domínio, escopo e arquitetura definidos. Agora é hora de gerar a primeira versão do código para as funcionalidades Phase 1.

Instrução para o assistente IA:
- Gere uma proposta inicial de implementação para as features Phase 1 (`patient-master`, `capacity-model`, `scoring-algorithm`, `booking-system`, `staff-dashboard`). Priorize clareza estrutural, separação em camadas e aderência ao domínio canônico.
- Inclua uma divisão por módulos, classes principais e responsabilidades, sem detalhar tudo de uma vez. Se houver ambiguidade, assuma a solução mais simples compatível com o `CONTEXT.md` e as ADRs.

Saída esperada:
- Lista de classes, pacotes e funções principais para o primeiro ciclo de implementação.
- Trechos de código iniciais ou esqueleto de arquivos prontos para evoluir no ciclo 2.
